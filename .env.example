# Google API Keys (REQUIRED)
# Obtain from https://ai.google.dev/
GOOGLE_API_KEY=your_api_key_here

# MCP Server Paths (REQUIRED)
# These paths must point to the actual directories containing the MCP servers
# Absolute paths are recommended to avoid issues with relative paths
PLANNING_MCP_PATH=/absolute/path/to/agent-planner-mcp
CONTEXT7_MCP_PATH=/absolute/path/to/context7-mcp
FILESYSTEM_MCP_PATH=/absolute/path/to/filesystem-mcp
PLAYWRIGHT_MCP_PATH=/absolute/path/to/playwright-mcp
WEBSEARCH_MCP_PATH=/absolute/path/to/websearch-mcp

# Planning System API (REQUIRED)
# The API URL defaults to localhost:3000 if not specified
PLANNING_API_URL=http://localhost:3000
# Generate an API token from the planning system UI or API
PLANNING_API_TOKEN=your_api_token_here

# INSTRUCTIONS FOR SETUP:
# 1. Copy this file to .env
# 2. Replace all placeholder values with your actual values
# 3. Ensure all paths exist and are accessible
# 4. Make sure the MCP servers are installed and configured
# 5. Verify the planning system API is running at the specified URL
# 6. Test the connection with: python test_mcp_tools.py
