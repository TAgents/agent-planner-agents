# Google API Keys (REQUIRED)
# Obtain from https://ai.google.dev/
GOOGLE_API_KEY=your_api_key_here

# MCP Server Paths
# Path to the planning system MCP server directory
PLANNING_MCP_PATH=/Users/michmalk/dev/talkingagents/agent-planner-mcp

# Workspace path for filesystem MCP server
WORKSPACE_PATH=/Users/michmalk/dev/talkingagents

# Planning System API (REQUIRED)
# The API URL defaults to localhost:3000 if not specified
PLANNING_API_URL=http://localhost:3000
# Generate an API token from the planning system UI or API
PLANNING_API_TOKEN=your_api_token_here

# INSTRUCTIONS FOR SETUP:
# 1. Copy this file to .env
# 2. Replace all placeholder values with your actual values
# 3. Ensure the planning MCP server path exists and is accessible
# 4. Make sure the planning system API is running at the specified URL
# 5. Ensure you have npm/npx installed for the specialized MCP servers
# 6. Test the connection with: python test_mcp_tools.py
